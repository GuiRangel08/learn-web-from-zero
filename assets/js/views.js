(() => {
  const F = FirstCode, e = value => F.escape(value);
  const progressBar = (value, label) => `<progress max="100" value="${value}" aria-label="${e(label)}">${Math.round(value)}%</progress>`;
  const resumeLink = state => state.introDone ? `#/aula/${F.progress.resume(state).id}` : '#/introducao';
  const lessonLink = (lesson, state) => {
    const unlocked = F.progress.isUnlocked(state, lesson), entry = state.lessons[lesson.id];
    const status = entry?.completed ? 'Concluída' : entry ? 'Em andamento' : 'Não iniciada';
    const content = `<span class="lesson-status ${entry?.completed ? 'done' : ''}" aria-hidden="true">${entry?.completed ? '✓' : String(lesson.order).padStart(2, '0')}</span><span>${e(lesson.title)}<small>${unlocked ? status : 'Conclua a aula anterior'}</small></span>`;
    return `<li>${unlocked ? `<a href="#/aula/${lesson.id}" class="lesson-link ${state.currentLesson === lesson.id && location.hash.startsWith('#/aula/') ? 'active' : ''}">${content}</a>` : `<span class="lesson-link locked">${content}</span>`}</li>`;
  };
  F.views = {
    sidebar(state) {
      const completed = F.lessons.filter(l => state.lessons[l.id]?.completed).length;
      const current = F.lessons.find(l => l.id === state.currentLesson);
      return `<a class="brand" href="#/"><span class="brand-mark" aria-hidden="true">&lt;/&gt;</span><span>primeiro<span class="accent">código</span><small>Pequenos passos. Novas possibilidades.</small></span></a>
        <nav aria-label="Navegação principal"><a class="nav-item" href="#/painel">▦ <span>Meu aprendizado</span></a><a class="nav-item" href="#/introducao">◎ <span>Antes de começar</span></a><p class="eyebrow nav-label">SEU CAMINHO</p>
        ${F.modules.map(module => `<details class="module-nav" ${module.id === (current?.moduleId || 'html') ? 'open' : ''}><summary><span class="module-icon">${e(module.symbol)}</span>${e(module.title)}</summary><ol>${F.lessons.filter(l => l.moduleId === module.id).map(l => lessonLink(l, state)).join('')}</ol></details>`).join('')}</nav>
        <div class="sidebar-bottom"><div class="between"><span>Progresso geral</span><strong>${state.percentage}%</strong></div>${progressBar(state.percentage, 'Progresso de todo o catálogo')}<small>${completed} de ${F.lessons.length} aulas</small><a href="#/configuracoes" class="nav-item">⚙ <span>Preferências e seus dados</span></a></div>`;
    },
    shell(state) {
      return `<div class="app-shell ${state.preferences.sidebar ? '' : 'sidebar-hidden'}"><aside id="sidebar" class="sidebar" aria-label="Trilha de aprendizado">${this.sidebar(state)}</aside><div class="main-shell">
        <header class="topbar"><div class="topbar-start"><button id="toggle-sidebar" class="icon-button" aria-label="Mostrar ou recolher navegação" aria-expanded="${state.preferences.sidebar}">☰</button><span class="topbar-title">Seu espaço para aprender</span></div><div class="topbar-actions"><span class="streak" title="Dias consecutivos de atividade local">◷ <span id="streak-count">${F.progress.streak(state)}</span> dias de prática</span><button id="theme-toggle" class="icon-button" aria-label="Alternar tema claro e escuro">◐</button><span class="avatar" aria-label="Perfil local">EU</span></div></header>
        <main id="main" tabindex="-1"></main><footer class="app-footer">Feito para aprender, no seu ritmo. <span>Seu progresso fica neste navegador.</span></footer></div></div>`;
    },
    home(state) {
      return `<section class="home page"><p class="eyebrow"><span class="tiny-dot"></span> SEU PRIMEIRO PASSO NA WEB</p><div class="hero"><div><h1>Você também pode<br><span class="accent">criar com código.</span></h1><p class="lead">Do primeiro “olá” à sua própria página.<br>Aprenda fazendo, um pequeno passo de cada vez.</p><div class="hero-actions"><a class="button primary" href="${resumeLink(state)}">${state.introDone ? 'Continuar aprendendo' : 'Começar a aprender'} →</a><a class="button subtle" href="#/painel">Explorar o caminho</a></div><p class="muted small">Sem experiência prévia. Sem pressa. Sem instalações.</p></div>
        <div class="hero-demo"><div class="demo-top"><span class="window-dots" aria-hidden="true">● ● ●</span><span>Sua primeira descoberta</span></div><div class="demo-code"><span class="muted">1</span> <span class="code-tag">&lt;h1&gt;</span>Olá, mundo!<span class="code-tag">&lt;/h1&gt;</span></div><div class="demo-result"><span>O NAVEGADOR MOSTRA</span><h2>Olá, mundo!</h2><p>Uma linha de código. Um novo começo.</p></div><div class="demo-bottom"><span class="accent">✓</span> É assim que começa.</div></div></div>
        <div class="feature-grid"><article><span class="feature-number">01</span><h2>Entenda primeiro</h2><p>Explicações curtas e exemplos que fazem sentido no dia a dia.</p></article><article><span class="feature-number">02</span><h2>Experimente de verdade</h2><p>Escreva seu código e veja o resultado, aqui mesmo.</p></article><article><span class="feature-number">03</span><h2>Avance com confiança</h2><p>Receba orientações claras e acompanhe cada conquista.</p></article></div>
        <div class="section-heading"><div><p class="eyebrow">UM PASSO DE CADA VEZ</p><h2>Seu caminho de aprendizado</h2></div><span class="pill">${F.lessons.filter(l => l.available).length} aulas práticas disponíveis</span></div>${this.moduleCards(state)}</section>`;
    },
    moduleCards(state) {
      return `<div class="module-grid">${F.modules.map((module, index) => {
        const stats = F.progress.moduleStats(state, module.id);
        const lessons = F.lessons.filter(l => l.moduleId === module.id);
        const next = lessons.find(l => !state.lessons[l.id]?.completed) || lessons[0];
        const unlocked = F.progress.isUnlocked(state, next);
        return `<article class="module-card"><div class="between"><span class="module-badge ${module.id}">${e(module.symbol)}</span><span class="muted small">MÓDULO 0${index + 1}</span></div><h3>${e(module.title)}</h3><p>${e(module.description)}</p><div class="between small"><span>${stats.completed} / ${stats.total} aulas</span><span>${stats.available} publicadas</span></div>${progressBar(stats.completed / stats.total * 100, module.title)}${unlocked ? `<a href="${state.introDone ? `#/aula/${next.id}` : '#/introducao'}" class="text-link">${stats.completed === stats.total ? 'Revisar módulo' : stats.completed ? 'Continuar módulo' : 'Começar módulo'} →</a>` : '<p class="small muted">Conclua o módulo anterior para começar.</p>'}</article>`;
      }).join('')}</div>`;
    },
    dashboard(state) {
      const count = F.lessons.filter(l => state.lessons[l.id]?.completed).length, next = F.progress.resume(state);
      return `<section class="page"><p class="eyebrow">MEU APRENDIZADO</p><h1>Cada passo conta.</h1><p class="lead">${count ? 'Você já transformou ideias em código. Continue sua jornada.' : 'Seu caminho começa com uma pequena descoberta.'}</p><div class="stats-grid"><article class="stat"><strong>${count}<span>/ ${F.lessons.length}</span></strong><p>Aulas concluídas</p>${progressBar(count / F.lessons.length * 100, 'Percurso completo')}</article><article class="stat"><strong>${F.progress.streak(state)} <span>dias</span></strong><p>Sequência de prática</p></article><article class="stat"><strong>${state.percentage}<span>%</span></strong><p>Do percurso HTML, CSS e JavaScript</p></article></div>
        <div class="continue-card"><div><span class="eyebrow">${count === F.lessons.length ? 'PERCURSO CONCLUÍDO' : 'SEU PRÓXIMO PASSO'}</span><h2>${e(next.title)}</h2><p>${state.lastActivity ? `Última atividade: ${new Date(state.lastActivity).toLocaleString('pt-BR')}` : 'Seu progresso será salvo automaticamente neste navegador.'}</p></div><a class="button primary" href="${resumeLink(state)}">${count === F.lessons.length ? 'Revisar' : 'Continuar aprendendo'} →</a></div>${this.moduleCards(state)}</section>`;
    },
    intro(state) {
      const step = state.introStep, item = F.intro[step];
      return `<section class="page intro-page"><a class="text-link" href="#/">← Início</a><p class="eyebrow">ANTES DO PRIMEIRO CÓDIGO · ${step + 1} DE ${F.intro.length}</p>${progressBar((step + 1) / F.intro.length * 100, 'Introdução')}<article class="intro-card"><span class="lesson-kicker">UMA DESCOBERTA POR VEZ</span><h1>${e(item[0])}</h1><p class="lead">${e(item[1])}</p>${step === F.intro.length - 1 ? '<pre><code>&lt;h1&gt;Olá, mundo!&lt;/h1&gt;</code></pre><div class="example-result"><small>RESULTADO NO NAVEGADOR</small><h2>Olá, mundo!</h2></div>' : ''}</article><div class="between"><button id="intro-back" class="button secondary" ${step === 0 ? 'disabled' : ''}>← Anterior</button><button id="intro-next" class="button primary">${step === F.intro.length - 1 ? 'Praticar meu primeiro código' : 'Entendi, próximo passo'} →</button></div></section>`;
    },
    lesson(lesson, state) {
      const entry = F.progress.entry(state, lesson), next = F.lessons.find(l => l.id === lesson.unlocks[0]);
      const module = F.modules.find(m => m.id === lesson.moduleId), languages = F.progress.languages(state, lesson);
      return `<section class="lesson-page"><div class="lesson-heading"><div><p class="eyebrow">${e(module.title)} <span class="muted">/ AULA ${String(lesson.order).padStart(2, '0')}</span></p><h1>${e(lesson.title)}</h1><p>${e(lesson.description)}</p></div><span class="pill">◷ Cerca de ${lesson.duration}</span></div>
        <div class="mobile-tabs" role="tablist" aria-label="Áreas da aula">${['Aula', 'Código', 'Resultado', 'Avaliação'].map((name, i) => `<button role="tab" id="mobile-tab-${i}" data-panel="${i}" aria-controls="lesson-panel-${i}" aria-selected="${i === 0}">${name}</button>`).join('')}</div>
        <div class="workspace" data-active-panel="0"><section id="lesson-panel-0" class="panel theory-panel" aria-labelledby="theory-heading"><div class="panel-heading"><h2 id="theory-heading">01 <span>A descoberta</span></h2><span class="small muted">Leia e experimente</span></div><div class="theory-body">
        ${lesson.theory.map(([title, text], i) => `<${i < 2 ? 'div' : 'details'} class="theory-block" ${i === 2 ? 'open' : ''}>${i < 2 ? `<h3>${e(title)}</h3>` : `<summary>${e(title)}</summary>`}<p>${e(text)}</p></${i < 2 ? 'div' : 'details'}>`).join('')}
        <h3>Sintaxe mínima <small>· como escrever</small></h3><pre><code>${e(lesson.syntax)}</code></pre><details class="theory-block"><summary>Um exemplo pequeno</summary><pre><code>${e(lesson.example)}</code></pre><p><strong>Resultado esperado:</strong> ${e(lesson.visual)}</p><iframe class="example-frame" title="Resultado visual do exemplo da aula" sandbox="allow-scripts" referrerpolicy="no-referrer"></iframe></details>
        <div class="challenge-description"><span class="eyebrow">AGORA É COM VOCÊ</span><h3>${e(lesson.challenge.title)}</h3><p>${e(lesson.challenge.description)}</p><ul>${lesson.challenge.requirements.map(r => `<li>${e(r.description)}</li>`).join('')}</ul></div>
        <div class="hints"><h3>Precisa de um empurrãozinho?</h3><ol id="hint-list">${lesson.challenge.hints.slice(0, entry.hints).map(h => `<li>${e(h)}</li>`).join('')}</ol><button id="hint-button" class="button secondary small" ${entry.hints === 3 ? 'disabled' : ''}>Ver dica ${Math.min(3, entry.hints + 1)} de 3</button></div>
        <details class="theory-block"><summary>Erros comuns e boas práticas</summary><ul>${[...lesson.commonMistakes, ...lesson.bestPractices].map(t => `<li>${e(t)}</li>`).join('')}</ul></details><details class="theory-block"><summary>Resumo e próximo passo</summary><p>${e(lesson.summary)}</p><p>${next ? `Próxima aula: ${e(next.title)}.` : 'Você chegou ao projeto final. Revise as aulas quando quiser.'}</p></details><button id="solution-button" class="text-button">Consultar solução de referência</button><div id="solution-area"></div></div></section>
        <div class="resize-handle" id="theory-resize" role="separator" aria-label="Largura do conteúdo da aula" aria-orientation="vertical" aria-valuemin="20" aria-valuemax="45" aria-valuenow="30" tabindex="0"></div>
        <section id="lesson-panel-1" class="panel code-panel" aria-labelledby="code-heading"><div class="panel-heading"><h2 id="code-heading">02 <span>Seu código</span></h2><span id="save-state" class="save-state" role="status">Salvo neste navegador</span></div><div class="editor-toolbar"><button id="run-code" class="button primary small">▷ Executar</button><button id="restore-code" class="button secondary small">Restaurar</button><button id="copy-code" class="button secondary small">Copiar</button><button id="clear-code" class="text-button small">Limpar</button></div><div id="editor"></div><div class="editor-bottom"><span class="tiny-dot"></span> ${e(languages.map(l => l.toUpperCase()).join(' · '))} <span class="muted">· linguagens nativas da web</span></div></section>
        <div class="resize-handle" id="preview-resize" role="separator" aria-label="Largura da área de resultado" aria-orientation="vertical" aria-valuemin="22" aria-valuemax="48" aria-valuenow="32" tabindex="0"></div>
        <div class="result-column"><section id="lesson-panel-2" class="panel preview-panel" aria-labelledby="preview-heading"><div class="panel-heading"><h2 id="preview-heading">03 <span>O resultado</span></h2><button id="refresh-preview" class="icon-button" aria-label="Atualizar pré-visualização">↻</button></div><div class="preview-toolbar"><div class="device-buttons" role="group" aria-label="Tamanho da pré-visualização"><button data-device="desktop" aria-pressed="true" title="Desktop">Desktop</button><button data-device="tablet" aria-pressed="false" title="Tablet, 768 pixels">Tablet</button><button data-device="mobile" aria-pressed="false" title="Celular, 375 pixels">Celular</button></div><label class="small auto-label"><input type="checkbox" id="auto-run" ${state.preferences.autoRun ? 'checked' : ''}> Automático</label></div><div class="preview-viewport" data-device="desktop"><iframe id="preview-frame" title="Resultado do código escrito por você" sandbox="allow-scripts" referrerpolicy="no-referrer" allow="camera 'none'; microphone 'none'; geolocation 'none'; clipboard-read 'none'; clipboard-write 'none'"></iframe></div><div id="preview-errors" class="error-messages" role="status" aria-live="polite"></div><p class="preview-note">Ambiente isolado · links e envios externos bloqueados</p></section>
        <div class="horizontal-resize" role="separator" tabindex="0" aria-label="Altura da pré-visualização" aria-orientation="horizontal" aria-valuemin="180" aria-valuemax="700" aria-valuenow="320"></div><section id="lesson-panel-3" class="panel assessment-panel" aria-labelledby="assessment-heading"><div class="panel-heading"><h2 id="assessment-heading">04 <span>Seu desafio</span></h2></div><div id="assessment"></div><div class="assessment-actions"><button id="verify-code" class="button secondary">Verificar desafio</button><span id="attempt-count" class="small muted">${entry.attempts} avaliações</span></div></section></div></div></section>`;
    },
    assessment(result, lesson, entry) {
      const states = { neutral: ['○', 'Ainda não avaliado', 'Faça uma alteração ou use “Verificar desafio” para receber orientações.'], far: ['↗', 'Vamos por partes', 'Comece pelo próximo pequeno passo abaixo.'], almost: ['◐', 'Quase completo', 'Você está construindo sua página. Falta pouco!'], complete: ['✓', 'Desafio completo!', 'Parabéns! Você transformou uma ideia em código.'] };
      const [icon, title, description] = states[result.status];
      const next = F.lessons.find(l => l.id === lesson.unlocks[0]);
      return `<div class="assessment-summary ${result.status}" role="status" aria-live="polite"><span class="assessment-icon" aria-hidden="true">${icon}</span><div><strong>${title}</strong><p>${result.status === 'neutral' ? 'Nenhuma avaliação deste código.' : `${result.passed} de ${result.total} requisitos atendidos · ${Math.round(result.percentage)}%`}</p></div></div><p class="assessment-description">${description}</p>
        <ul class="requirements">${result.results.map(r => `<li class="${r.state}"><span aria-hidden="true">${r.state === 'completed' ? '✓' : r.state === 'partial' ? '◐' : '○'}</span><div><strong>${e(r.description)}</strong><small>${r.state === 'completed' ? 'Concluído' : r.state === 'partial' ? 'Parcial' : 'Pendente'}${result.status === 'neutral' ? '' : ` — ${e(r.message)}`}</small></div></li>`).join('')}</ul>
        ${result.status === 'complete' ? `<div class="congratulations"><strong>Um novo passo conquistado.</strong><p>${e(lesson.summary)}</p>${next ? `<a class="button primary" href="#/aula/${next.id}">Próxima aula →</a><p class="small">${e(next.title)}</p>` : `<a class="button primary" href="#/painel">Ver minhas conquistas →</a><p class="small">Você concluiu as ${F.lessons.length} aulas de HTML, CSS e JavaScript!</p>`}</div>` : ''}${entry.solutionViewed ? '<p class="small muted solution-record">Solução de referência consultada nesta aula.</p>' : ''}`;
    },
    settings(state) {
      return `<section class="page settings-page"><p class="eyebrow">DO SEU JEITO</p><h1>Preferências e seus dados</h1><article class="settings-card"><h2>Seu espaço de escrita</h2><label for="font-size">Tamanho da letra no editor</label><select id="font-size">${[13, 15, 17, 19].map(s => `<option value="${s}" ${state.preferences.fontSize === s ? 'selected' : ''}>${s} pixels</option>`).join('')}</select><p class="muted">O tema pode ser alterado pelo botão ◐ no topo da página.</p></article>
        <article class="settings-card"><h2>Leve seu progresso com você</h2><p>Seu código, dicas, conclusões e preferências ficam apenas neste navegador. Exporte uma cópia para guardar ou continuar em outro dispositivo.</p><div class="action-row"><button id="export-progress" class="button primary">Exportar progresso</button><label class="button secondary file-button">Importar progresso<input id="import-progress" type="file" accept=".json,application/json"></label></div><p class="small muted">Importar substitui o progresso atual após sua confirmação. Arquivos aceitos: JSON da plataforma, até 5 MB.</p></article>
        <article class="settings-card"><h2>Recomeçar</h2><p>Apaga o progresso salvo neste navegador. Exporte uma cópia antes, se quiser manter seu trabalho.</p><button id="erase-progress" class="button danger">Apagar meu progresso</button></article></section>`;
    }
  };
})();
